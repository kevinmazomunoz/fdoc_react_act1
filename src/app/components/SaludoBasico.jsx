/**
 * Componente: SaludoBasico
 * Propósito:
 *   Muestra un saludo simple utilizando un nombre recibido por props.
 *
 * Props esperadas:
 *   - nombre: string (requerida)
 *   - entusiasta: boolean (opcional)
 *
 * Especificaciones:
 *   - Debe renderizar: "Hola, {nombre}".
 *   - Si entusiasta === true, agregar un énfasis como "!!!" o estilo especial.
 *   - Casos a considerar:
 *        a) <SaludoBasico nombre="Kevin" entusiasta={true} />
 *        b) <SaludoBasico nombre="Maria" />
 *
 * IMPORTANTE:
 *   Este archivo solo contiene documentación.
 *   El componente no está implementado, solo existe una estructura vacía
 *   para evitar errores en Next.js.
 */

export default function SaludoBasico() {
  return null; // Componente vacío pero válido
}
